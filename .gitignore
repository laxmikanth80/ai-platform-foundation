# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl is intentionally NOT ignored — commit it, per Terraform's
# own guidance, so provider version selection is reproducible across machines.

# AWS
.aws-sam/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
